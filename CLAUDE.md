# KaigaiQ

## 概要
海外就労・求人情報サービス。サンリー合同会社運営。
サイト：kaigaiq.com
LINE公式：@637hamys

## 技術構成
- GitHub Pages（legacy build）で配信。`_headers`/`_redirects` はCloudflare形式のため本番では無効
- 静的HTML
- GA4：G-HP8686808M（全ページ統一。管理アカウントはローカルのパスワード管理を参照。旧記載のG-L3F0B833S2は不使用）
- GSC：https://kaigaiq.com/ プロパティ。運用アカウント（3月〜）＋ オーナー個人アカウント（2026-07-05所有権追加、確認ファイル google823549b64d1f975d.html は削除禁止）
- IndexNow APIキー設定済み（キーファイルの所在は要確認）

## デザイン
- ブランドカラー：ブラック×ゴールド
- LINEリッチメニュー設定済み（3パネル：求人を見る／無料相談／掲載申し込み）

## 過去の主な対応履歴
- Service Worker キャッシュ問題をHTTPS有効化で解決
- LINEリッチメニュー用SVG→PNG変換（Node.js/sharp使用）
- PageSpeed 100達成済み（Google Fonts削除・システムフォント置換）

## 内部トラフィック除外（GA4・2026-09-21導入）

オーナー自身の動作確認アクセスがCV数値に混入するのを防ぐ仕組み。GA4管理画面のIPフィルタではなく、
サイト側のフラグ方式（IP変動・端末移動の影響を受けないため）。

- **停止**: `https://kaigaiq.com/?noga=1` を一度開く → localStorage に `kq_noga=1` が保存され、
  以後そのブラウザでは `window['ga-disable-G-HP8686808M'] = true` が立ちGA4へ送信されない
- **解除**: `?noga=0`
- **確認**: 停止中はどのページでもコンソールに `[KaigaiQ] GA4計測 停止中` と出る。
  出ていなければ効いていない。localStorageが使えない環境では警告が出る
- **実装**: 全89ページの `<head>`、gtag.js の**直前**にインラインで埋め込み済み。
  静的13ページは直接、area/news は `tools/build-*-pages.js` のテンプレート、
  shop 38件は `shop.html` 経由で生成される

### 新規ページを追加するときの必須手順
gtag.js を読み込むページを増やしたら、**同じスニペットを gtag.js より前に入れる**。
後ろに置くと page_view を止められない。ビルド生成物は該当ビルドスクリプトのテンプレートに入れる。

### 制約・禁止
- **ブラウザ単位・端末単位**。PC・スマホそれぞれで一度 `?noga=1` を踏む必要がある。
  シークレットウィンドウやCookie削除でリセットされる
- **遡及しない**。導入以前のデータからは除外されない
- **`?noga=1` 付きのURLを外部へ共有しない**（LINE・SNS・メール等）。
  受け取った一般ユーザーは以後恒久的に計測から消え、事後に気づくことも戻すこともできない

## 注意事項
- 別サイト（GitHub Pages）と連携あり。対象と内容はローカルの運用メモを参照
- GA4は別サイトと測定IDを分けて管理（取り違え注意）
